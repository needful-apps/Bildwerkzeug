services:
  bildwerkzeug:
    image: ghcr.io/needful-apps/bildwerkzeug:latest
    # Alternativ lokal bauen:
    # build: .
    container_name: bildwerkzeug
    restart: unless-stopped
    ports:
      - "5050:5000"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=${SECRET_KEY:-change-this-in-production}
      - ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
      - ADMIN_EMAIL=${ADMIN_EMAIL:-admin@localhost}
      - DATABASE_URL=sqlite:////app/data/bildwerkzeug.db
      - MAX_UPLOAD_MB=50
      - SESSION_LIFETIME_HOURS=24
      - LOGIN_REQUIRED=${LOGIN_REQUIRED:-true}
    volumes:
      - bildwerkzeug_data:/app/data
      - bildwerkzeug_uploads:/app/uploads
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/login"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  bildwerkzeug_data:
  bildwerkzeug_uploads:
